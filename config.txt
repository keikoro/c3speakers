[db]
# absolute path to directory where db file(s) should be saved
# if left unspecified, the current working directory is used
# format without quotes:
# dir_path = /Users/your_username/some_directory/
dir_path = None

# names for database and table to save values into
db_name = speakers
table = speakers